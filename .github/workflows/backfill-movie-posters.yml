name: Backfill movie posters

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Dry run (no OTS writes)"
        required: false
        default: false
        type: boolean
      refresh_all:
        description: "Refresh every poster from Douban"
        required: false
        default: false
        type: boolean
      refresh_broken:
        description: "Re-fetch broken poster URLs"
        required: false
        default: true
        type: boolean
      with_actors:
        description: "Fill missing actors from Douban"
        required: false
        default: true
        type: boolean
      limit:
        description: "Max movies (0 = all)"
        required: false
        default: "0"
        type: string

concurrency:
  group: backfill-movie-posters
  cancel-in-progress: false

jobs:
  backfill:
    runs-on: ubuntu-latest
    environment: personal-dashboard
    timeout-minutes: 120
    env:
      OTS_ENDPOINT: https://pd-dashboard.cn-shanghai.ots.aliyuncs.com
      OTS_INSTANCE_NAME: pd-dashboard
      POSTER_PUBLIC_BASE: https://pd.huhansen.cn
      OSS_WEB_BUCKET: huhansen-web
      OSS_ENDPOINT: oss-cn-shanghai.aliyuncs.com
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r scripts/requirements.txt

      - name: Assume provision role
        id: sts
        env:
          ALIBABA_CLOUD_ACCESS_KEY_ID: ${{ secrets.ALIBABA_CLOUD_ACCESS_KEY_ID }}
          ALIBABA_CLOUD_ACCESS_KEY_SECRET: ${{ secrets.ALIBABA_CLOUD_ACCESS_KEY_SECRET }}
          ALIBABA_CLOUD_ROLE_ARN: ${{ secrets.ALIBABA_CLOUD_ROLE_ARN }}
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq jq
          wget -q https://aliyuncli.alicdn.com/aliyun-cli-linux-latest-amd64.tgz
          tar -xzf aliyun-cli-linux-latest-amd64.tgz
          sudo mv aliyun /usr/local/bin/
          aliyun configure set \
            --profile default \
            --mode AK \
            --access-key-id "$ALIBABA_CLOUD_ACCESS_KEY_ID" \
            --access-key-secret "$ALIBABA_CLOUD_ACCESS_KEY_SECRET" \
            --region cn-shanghai
          CREDS=$(aliyun sts AssumeRole \
            --RoleArn "$ALIBABA_CLOUD_ROLE_ARN" \
            --RoleSessionName "github-backfill-posters" \
            --DurationSeconds 3600)
          echo "access_key_id=$(echo "$CREDS" | jq -r '.Credentials.AccessKeyId')" >> "$GITHUB_OUTPUT"
          echo "access_key_secret=$(echo "$CREDS" | jq -r '.Credentials.AccessKeySecret')" >> "$GITHUB_OUTPUT"
          echo "security_token=$(echo "$CREDS" | jq -r '.Credentials.SecurityToken')" >> "$GITHUB_OUTPUT"

      - name: Backfill posters
        env:
          ALIBABA_CLOUD_ACCESS_KEY_ID: ${{ steps.sts.outputs.access_key_id }}
          ALIBABA_CLOUD_ACCESS_KEY_SECRET: ${{ steps.sts.outputs.access_key_secret }}
          ALIBABA_CLOUD_SECURITY_TOKEN: ${{ steps.sts.outputs.security_token }}
        run: |
          ARGS=()
          [ "${{ inputs.dry_run }}" = "true" ] && ARGS+=(--dry-run)
          [ "${{ inputs.refresh_all }}" = "true" ] && ARGS+=(--all)
          [ "${{ inputs.refresh_broken }}" = "true" ] && ARGS+=(--refresh-broken)
          [ "${{ inputs.with_actors }}" = "true" ] && ARGS+=(--with-actors)
          LIMIT="${{ inputs.limit }}"
          [ -n "$LIMIT" ] && [ "$LIMIT" != "0" ] && ARGS+=(--limit "$LIMIT")
          python scripts/backfill_movie_posters.py --stack cn-shanghai "${ARGS[@]}"
